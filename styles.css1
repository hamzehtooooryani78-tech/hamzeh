/* styles.css — تنظیمات پایه، راست‌به‌چپ و واکنش‌گرا */
:root{
  --accent: #0b74de;
  --bg: #ffffff;
  --muted: #6b7280;
  --card: #f8fafc;
  --contrast: #111827;
  --radius: 12px;
  --container: 1100px;
  font-family: "Vazirmatn", system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
  color-scheme: light;
  -webkit-font-smoothing:antialiased;
  -moz-osx-font-smoothing:grayscale;
}

*{box-sizing:border-box}
html,body{height:100%}
body{
  margin:0;
  background:var(--bg);
  color:var(--contrast);
  line-height:1.6;
  direction:rtl;
}

/* layout container */
.container{
  max-width:var(--container);
  margin:0 auto;
  padding:1rem;
}

/* header */
.site-header{
  background:linear-gradient(180deg, rgba(11,116,222,0.06), transparent);
  position:sticky; top:0; z-index:40;
  backdrop-filter: blur(4px);
}
.header-inner{
  display:flex;
  align-items:center;
  justify-content:space-between;
  gap:1rem;
}
.logo{display:flex;align-items:center;gap:.6rem;text-decoration:none;color:var(--contrast);font-weight:700}
.logo img{border-radius:8px}
.main-nav{display:flex;gap:.8rem;align-items:center}
.main-nav a{text-decoration:none;color:var(--muted);padding:.5rem .7rem;border-radius:8px}
.main-nav a:hover{color:var(--contrast);background:rgba(0,0,0,0.03)}
.btn-outline{border:1px solid rgba(0,0,0,0.08);padding:.45rem .7rem;border-radius:8px;background:transparent;color:var(--contrast);text-decoration:none}
.nav-toggle{display:none;border:0;background:transparent;font-size:1.4rem}

/* hero */
.hero{padding:3rem 0}
.hero-grid{display:grid;grid-template-columns:1fr 420px;gap:1.5rem;align-items:center}
.hero-text h1{font-size:2rem;margin:0 0 .6rem}
.hero-text p{color:var(--muted);margin:.3rem 0 1rem}
.hero-ctas{display:flex;gap:.7rem;flex-wrap:wrap}
.btn-primary{background:var(--accent);color:white;padding:.6rem 1rem;border-radius:10px;text-decoration:none}
.btn-primary:hover{opacity:.95}
.features{display:flex;gap:.8rem;list-style:none;padding:0;margin:1rem 0 0;color:var(--muted)}
.device-mockup img{width:100%;max-width:420px;border-radius:14px;box-shadow:0 8px 30px rgba(2,6,23,0.08)}

/* sections */
.section{padding:2.2rem 0}
.section-title{font-size:1.25rem;margin:0 0 1rem}

/* cards */
.cards-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(220px,1fr));gap:1rem}
.card{background:var(--card);padding:1rem;border-radius:12px;box-shadow:0 6px 18px rgba(11,20,40,0.03)}
.card h3{margin:0 0 .5rem}

/* products */
.bg-light{background:#fbfdff}
.products-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(200px,1fr));gap:1rem}
.product{background:white;padding:1rem;border-radius:12px;text-align:center;border:1px solid rgba(0,0,0,0.03)}
.product img{width:100%;height:140px;object-fit:cover;border-radius:8px}
.price{color:var(--accent);font-weight:700}

/* carousel */
.carousel{position:relative;display:flex;align-items:center;gap:1rem;overflow:hidden}
.carousel-track{display:flex;gap:1rem;transition:transform .45s cubic-bezier(.22,.9,.3,1)}
.testimonial{min-width:320px;background:linear-gradient(180deg,#fff,#fbfdff);padding:1rem;border-radius:12px;border:1px solid rgba(0,0,0,0.03)}
.carousel-btn{border:0;background:transparent;font-size:1.6rem;padding:.4rem;cursor:pointer}

/* contact */
.bg-contrast{background:linear-gradient(180deg,#0b74de, #0763b8);color:white}
.contact-grid{display:grid;grid-template-columns:1fr 420px;gap:1rem;align-items:start;padding:1.5rem 0}
.contact-info p, .contact-list{color:rgba(255,255,255,0.9)}
.contact-form{background:rgba(255,255,255,0.06);padding:1rem;border-radius:10px}
.contact-form label{display:block;margin:.5rem 0 .3rem}
.contact-form input,.contact-form textarea{
  width:100%;padding:.6rem;border-radius:8px;border:1px solid rgba(255,255,255,0.12);
  background:transparent;color:white;outline:none
}
.contact-form input::placeholder, .contact-form textarea::placeholder{color:rgba(255,255,255,0.55)}
.form-actions{display:flex;gap:.5rem;margin-top:.75rem}
.btn-sm{display:inline-block;padding:.4rem .6rem;border-radius:8px;background:rgba(11,116,222,0. 1);text-decoration:none;color:var(--accent)}
.form-note{margin-top:.6rem;color:lightgreen}

/* footer */
.site-footer{padding:1.2rem 0;background:#0b1220;color:#cbd5e1}
.footer-grid{display:flex;justify-content:space-between;align-items:center}
.social a{text-decoration:none;margin-left:.5rem;font-size:1.2rem}
copyright{display:block}

/* responsiveness */
@media (max-width:980px){
  .hero-grid,.contact-grid{grid-template-columns:1fr}
  .nav-toggle{display:block}
  .main-nav{display:none}
}

/* small screens */
@media (max-width:480px){
  .hero-text h1{font-size:1.4rem}
  .device-mockup img{max-width:280px}
  .testimonial{min-width:260px}
}
